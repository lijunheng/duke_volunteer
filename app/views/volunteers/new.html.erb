<% provide(:title, 'New Opportunity') %>
<h1><%= yield(:title) %></h1>
<div class="row">
  <div class="span6 offset3">
    <%= form_for([@organization, @volunteer]) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>

      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.label :description %>
      <%= f.text_area :description %>

      <%= f.submit "List volunteer opportunity", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>